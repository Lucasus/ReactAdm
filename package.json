{
  "name": "react-adm",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts-no-linting": "1.0.11"
  },
  "devDependencies": {
    "@types/node": "^8.0.34",
    "@types/react": "^16.0.10",
    "npm-run-all": "^4.1.1",
    "typescript": "^2.5.3"
  },
  "scripts": {
    "start": "run-p start-tsc start-react",
    "start-react": "react-scripts start",
    "start-tsc": "tsc -p ./tsconfig.json",
    "build": "npm run tsc-build & react-scripts build",
    "build-tsc": "tsc -p ./tsconfig.prod.json",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
